class Solution:
    def prevSmaller(self, arr):
        stack = []
        res = []
        
        for x in arr:
            while stack and stack[-1] >= x:
                stack.pop()
            
            if stack:
                res.append(stack[-1])
            else:
                res.append(-1)
            
            stack.append(x)
        
        return res
