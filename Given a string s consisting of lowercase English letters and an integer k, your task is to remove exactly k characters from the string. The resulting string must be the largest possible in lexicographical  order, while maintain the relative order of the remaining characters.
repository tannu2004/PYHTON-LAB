class Solution:
    def maxSubseq(self, s, k):
        stack = []
        
        for ch in s:
            
            while k > 0 and stack and stack[-1] < ch:
                stack.pop()
                k -= 1
            stack.append(ch)
        
    
        while k > 0:
            stack.pop()
            k -= 1
        
        return "".join(stack)
